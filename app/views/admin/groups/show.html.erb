<h1>グループ詳細</h1>

 <p>グループ名：<%= @group.title %></p>
 <p>紹介：<%= @group.introduction %><p>
 <p>グループ作成者:<%= @group.owner.nickname %></p>
 <% @group_members.each do |group_member| %>
  <p>グループ参加者:<%= group_member.user.nickname %></p>
 <% end %>
 <%= link_to 'グループ削除', admin_group_path(@group), method: :delete, data: { confirm: "本当に削除しますか？" } %>