<h1>グループ詳細</h1>

 <p>グループ名：<%= @group.title %></p>
 <p>紹介：<%= @group.introduction %><p>
 <p>グループ作成者:<%= @group.owner_id %></p>
 
  
  
  <% if @group.is_owned_by?(current_user) %>
    <%= link_to "編集", edit_group_path(@group) %>
  <% end %>
